# v0.9.7-D Design Consultation C - FINAL Re-Review (R3)

**Role:** senior product designer, design consultation C (third and final re-review; verdict on the RC2-01..RC2-10 remediation)
**Date:** 2026-08-06 - **Run:** `v0.9.7-d-20260805-r1`
**Inputs read in full and in order:** `docs/development/V0.9.7_D_STUDENT_DESIGN_SYSTEM.md` (R2, Status: APPROVED DRAFT - pending representative-page validation), `kimi_review_c2.md` (RC2-01..RC2-10), `RUN_VERIFICATION_V0.9.7_D_DESIGN_SYSTEM_FREEZE.md`, `rendered_page_matrix_evidence.json`, `d1_browser_matrix.py`.
**Screenshots reviewed:** all twelve captures (top/bottom/mid x en/zh x 1280x900/390x844); SHA-256 verified pairwise-distinct.
**Corroborating read-only inspection:** `app/ui/pixel_art.py` (RC2-01 label pinning L493-502, RC2-02 field-error token L1173-1176, RC2-03 dashed/empty split composites L983-1000, `.px-divider` L863-866, `.px-page-heading` L669-673, button shadow rules L433-470, media-query token scaling L614-636, cycle-card rule L1094-1101), `app/ui/components.py` (notice icon size=20 L250/275, badge 16 L205, empty 24 L394, `page_header` L27-38, `divider` L468-470), `app/ui/features/student/journey.py` (`student_page_intro` L332); pixel-level measurements (exact colors, border widths, shadows, glyph colors, geometry) on all twelve captures. No production code, tests, or locales were modified; no git commands were run.
**Method:** UI/UX Pro Max priority categories (1 accessibility/contrast, 2 interaction, 5 layout/responsive, 6 typography/color, professional-UI consistency) applied to the renders; every visual claim below is backed by measured pixels cross-checked against token contracts and cited source lines.

---

## 1. Verdict

**READY WITH DOCUMENTED LIMITATIONS** - all three gates RC2-01..RC2-03 are visibly resolved in the fresh captures and matched by computed-style assertions in all four locale/viewport combinations; doc items RC2-04..RC2-10 are resolved or process-correct at this stage. What remains (RC3-01..RC3-05) is doc-text precision and evidence hygiene only: no adopting page would inherit a visual or contractual defect, and each item folds into the already-scheduled freeze closure edit (doc section 20 verdict line + report section 14) without another review round. Freeze may be declared at D1.3 with the RC3 items recorded.

## 2. Gate resolution check (max 6)

- **RC2-01 - RESOLVED, pixel-verified.** The label color is now pinned at the label child (`[data-testid="stBaseButton-primary"] span/p { color: var(--px-action-text) !important; }`, `pixel_art.py:493-496`; secondary/download pinned to `--px-text`, L498-502), defeating the global body-color rule that caused ink-on-red. The harness asserts computed `rgb(255, 255, 255)` on both button and label (`d1_browser_matrix.py:238-267`). Measured in the fresh captures: white glyph cores and zero ink pixels inside the red fill in all four combinations (en desktop 52 white px, glyph bbox x748-831/y740-749; zh desktop 14; en mobile 212; zh mobile 162). Section 3's "white text (4.93:1)" row is now true in the render.
- **RC2-02 - RESOLVED, token-verified (render verification deferred, RC3-03).** `div.px-field-error` now declares `color: var(--px-status-on-error)` (`pixel_art.py:1173-1176`) = `#a30d3d` on white, 6.78:1 - the section 3 error-row contract; the near-invisible tint-on-white cascade is gone. Journey renders no field error, so this voice is code/token-verified only.
- **RC2-03 - RESOLVED, pixel-verified.** Both dashed rules now split the composite (`border: var(--px-border-thin); border-style: dashed;`) with explicit color overrides (`pixel_art.py:983-1000`); the harness asserts computed border-top `dashed`/`2px` on `.px-notice-dashed` (`d1_browser_matrix.py:268-278`). Measured dashed top edges on the legacy practice notice in all four combinations: 2px with ~52-53 dash runs on desktop (en bottom y=236-237, en mid y=190, zh bottom y=312) and ~33 runs on mobile (en y=73, zh y=176); fill/icon/text channels unchanged. Section 3's unavailable/legacy recipes now hold in the render.
- **RC2-04, RC2-06, RC2-07 - RESOLVED, render-verified.** Notice icons measure 20px (disclaimer glyph box 20px wide; `components.py:250,275`) and section 9 now scopes the bold-label channel to the badge. `.px-divider` is hardened 2px ink `!important` (`pixel_art.py:863-866`) and now paints: measured 2px ink rules below the disclaimer (en desktop y=438-439, span x510-1069) and in both sidebar separators (y=266-267, 502-503). The page-title rule is present and visible: `h2.px-page-heading` border-bottom measures 4px ink under "Learning Journey" on desktop (en y=546-549, zh y=472-475) and 2px on mobile (en y=527-528, zh y=466-467), matching the harness's 4px/2px assertion and section 9's "single sanctioned 4px structural line".
- **RC2-05 - RESOLVED in section 9; section 5 residue carried as RC3-02.** The Action block recipe (`student_action_block` = page-level next-step panel, 4px ink border + shadow-sm, one primary CTA) now exists in section 9 and matches measurement exactly (top border 4px ink y=610-613; bottom 4px border + 2px shadow y=705-710, en desktop bottom).
- **RC2-08, RC2-09, RC2-10 - RESOLVED.** Section 15 now carries KB-09 and the KB-13 mobile type-scale half. The freeze verification report exists and section 20 cites it; its section 14 correctly stays TBD until this verdict lands (process-correct). Twelve pairwise-distinct captures (SHA-256 verified) now include mid-scroll frames in which two-cycle separation is directly visible (cycle-1 card bottom border+shadow y=315-318, cycle-2 top border y=357-358, en desktop mid; symmetric in zh and mobile).

## 3. Consistency assessment (max 6)

- **Token discipline holds end-to-end.** One canonical `DESIGN_TOKENS` source feeds generated CSS; re-measured recipes still match section 3 in both locales: disclaimer `#fdf6d8` + 4px `#a16207` accent bar (x510-513) + tonal text; success/info/neutral badges unchanged; L2 cycle cards 2px ink + shadow-sm (right edge x1068-1071 = 2px border + 2px shadow); L3 hairlines; 720px column (`main_column_max_width` in the JSON).
- **Doc and render now agree on every re-measured RC2 claim:** white CTA labels, dashed 2px legacy borders, 20px notice icons, 2px ink dividers, the 4px/2px page-title rule, and the action-block recipe. The section 3 contract table has no visibly false rows left.
- **The section 5 mobile scaling sentence is exactly right** and now also covers the shadow: the media query (`pixel_art.py:614-636`) re-points `--px-border-thick` to 2px and `--px-shadow-md` to 2px 2px under 640px; the mobile CTA measures 2px border + 2px shadow (vs 2px + 4px desktop).
- **Bilingual parity is symmetric for every fix:** zh CTA label white on red, zh dashed edges, zh 4px/2px title rule, zh sans headings; no locale-specific artifacts introduced.
- **Two section 5 sentences remain measurably imprecise** (button resting shadow is md, not sm; the sanctioned-4px list omits the action block) - doc text only, carried as RC3-01/RC3-02; section 20's evidence bullet overstates the JSON artifact (RC3-05).
- **The freeze verification report is internally consistent with the repository state** (1158 passed / 8 skipped; 12 captures; commit list through `992363d` plus the pending closure commit); its open section 14 is the correct shape at review time.

## 4. Rollout-readiness statement

Ready for rollout. The shared layer every Student page will adopt - canonical tokens, quiet-state notice/badge recipes, keyed-container L2/L3 surface binding, the action hierarchy with a white-labelled single primary CTA, dashed unavailable/legacy channels, bilingual wrapping, the 44px guards, and the zero-write matrix harness with byte-distinct top/mid/bottom captures - is now verified end-to-end on the Journey representative page in all four locale/viewport combinations. Proceed to the Writing slice per section 19 in small page slices. When the first form-bearing page lands, extend the matrix with one rendered `.px-field-error` assertion so the RC2-02 fix becomes pixel-verified where it first matters (RC3-03). Fold RC3-01/02/04/05 into the freeze closure edit (two section 5 sentences, the section 9 gap figure, the section 20 evidence bullet, and report section 14) - all one-line documentation clarifications; no re-review round is required.

## 5. Remaining documented limitations

- **RC3-01 - Section 5 shadow sentence is false for buttons (doc text).** Section 5: "shadows sm (2px) on interactive cards and buttons". Rendered/measured: buttons rest at shadow-md (4px desktop, 2px mobile; `pixel_art.py:438,470`; en desktop CTA = 2px border + 4px shadow below y762; "Open Practice" = 6 ink rows y344-349); shadow-sm applies to buttons only on `:hover` (L446). Cycle cards and the action block do carry shadow-sm as written. One-line clarification at freeze; no visual change.
- **RC3-02 - Section 5's sanctioned-4px list omits the action block (RC2-05 residue).** Section 5 reserves 4px borders for "state accent bars, the focused panel, and structural rules"; the section 9 action block's 4px ink border (measured y=610-613) is a fourth use, documented in section 9 but not admitted into section 5's geometry sentence. Add it at freeze.
- **RC3-03 - The error-text voice remains unrendered in the evidence (inherited).** RC2-02's fix is token/code-verified, but no `.px-field-error` exists on Journey and the matrix asserts no rendered error state. The first form-adopting page (Writing) should add one rendered field-error capture + assertion so the contracted `#a30d3d` voice is pixel-verified where it is first user-visible.
- **RC3-04 - Section 9 "24px between cards" is the declared margin, not the rendered gap.** Code sets `margin-bottom: var(--px-space-6)` (24px; `pixel_art.py:1099`); measured inter-card whitespace is ~38-40px border-to-border including Streamlit container gaps (card-1 bottom y=315-318 -> card-2 top y=357-358, en desktop mid). The rhythm is uniform across cards; reword to "24px declared card margin" at freeze if precision is wanted.
- **RC3-05 - Section 20's JSON bullet overstates the artifact.** The harness asserts the white CTA label, dashed 2px border, and 4px/2px title rule (`d1_browser_matrix.py:238-289`) and the JSON records 0 exceptions - but the JSON persists only summary fields (`heading_sans`, `main_column_max_width`, `framed_containers`, screenshot paths); the computed values themselves are not in the file. Persist them at the next evidence refresh so section 20's parenthetical is literally true of the artifact.

## 6. Constraints respected in this review

No recommendation changes business behavior or wording semantics (RC3-01/02/04/05 are doc-text clarifications; RC3-03 is a future evidence assertion on an adopting page). No remote resources, no new features, no backend changes, no full mobile redesign, no dark mode, no animation. Every claim cites the component/token/capture and pixel measurement it was derived from.

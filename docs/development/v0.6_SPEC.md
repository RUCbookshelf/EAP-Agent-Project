# v0.6 SPEC — Progress Visualization and Versioned Configuration

Status: `completed`; verified with 149 passed, 1 opt-in live test skipped, migration 6 and `run.bat --verify` PASS. Stop before v0.7.

## Required outcome

Expose API-sourced student timelines, version-segmented metric trends, comparability/exclusion evidence, issue trajectories and Revision Comparison views. Add append-only non-sensitive configuration versions with validation, activation, rollback and audit; Analyzer/Metric/Algorithm/Prompt/Configuration registries; and scoped reanalysis preview/run workflows that do not call an LLM unless explicitly requested.

## Invariants

- Charts use FastAPI results and display submission, inclusion, versions and limitations; incompatible series are not silently connected.
- Exactly one main configuration is active. Editing creates a new version; invalid drafts cannot activate; rollback never deletes history.
- API keys and sensitive environment values never enter configuration payloads, database, API or UI.
- Reanalysis creates new AnalysisRuns/Snapshots, defaults to local analysis, previews scope, and never overwrites old results.
- Extension interfaces support future CALF families without a CALF total or proficiency field.
- Local-only administration is not suitable for public deployment.

## Acceptance evidence

Tests cover all 35 numbered v0.6 cases and all earlier suites, plus FastAPI, Streamlit, migration, persistence and `run.bat`. Produce the named visualization/configuration/reanalysis/CALF-extension docs, `RUN_VERIFICATION_V0.6.md`, both v0.6 review guides, an independent commit, a clean worktree, and stop before v0.7.

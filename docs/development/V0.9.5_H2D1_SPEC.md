# v0.9.5-H2D1 SPEC - Formalize ConfigurationPort as a Structural Protocol

**Status:** completed and fully verified (2026-08-03).

**Baseline:** `79c94bd` (v0.9.5-H2C verification), branch `master`.

**Stage type:** behavior-preserving contract-typing formalization. Not a
rename, not a relocation, not API Port annotation work, not a runtime
type-checking stage, not a static-type-tooling stage.

## 1. Current definition and consumer

- Definition: `app/services/configuration.py:14` -
  `class ConfigurationPort:` (plain structural class).
- Sole production consumer: `ConfigurationService.__init__`
  (`configuration.py:25`) - `repository: ConfigurationPort` annotation only.
- Intended concrete satisfier: `SQLiteConfigurationRepository`
  (`app/infrastructure/sqlite/repositories/configuration.py:12`), injected as
  the facade-owned instance by application composition (unchanged).

## 2. Exact seven-method signature inventory

All bodies are ellipsis-only declarations; full machine-readable parity:
`verification/v0.9.5-h2d1/configuration_port_before.json`.

| # | Method | Parameters (name / kind / default) | Return annotation |
| --- | --- | --- | --- |
| 1 | `list_configurations` | `self` (positional) | none |
| 2 | `get_configuration` | `self`, `configuration_id_or_version: str` (positional) | none |
| 3 | `get_active_configuration` | `self` (positional) | none |
| 4 | `create_configuration` | `self`, `request`, `parent_version` (positional) | none |
| 5 | `set_configuration_validation` | `self`, `configuration_id` (positional); `passed`, `errors`, `actor` (kw-only) | none |
| 6 | `activate_configuration` | `self`, `configuration_id` (positional); `actor`, `reason`, `action="activate"` (kw-only) | none |
| 7 | `list_configuration_audit` | `self`, `configuration_id=None` (positional, default) | none |

## 3. Proof it is annotation-only

- Production references: exactly two - the definition and the
  `ConfigurationService.__init__` annotation (`app/**` scan).
- Instantiation sites: **0** (AST scan of `app`, `tests`, `scripts`).
- Subclassing sites: **0**.
- Runtime `isinstance`/`issubclass` sites referencing the exact name: **0**
  (word-boundary scan; the only related hit is
  `ActiveConfigurationPort`, a different API-owned contract).
- No constructor, no runtime state, no serialization/DI/framework
  registration depends on the ordinary-class identity.

## 4. Intended concrete satisfier

`SQLiteConfigurationRepository` (never modified by H2D1; no explicit
inheritance of the Protocol). Parameter names, kinds, order, and defaults of
all seven methods match the Port declarations (verified by
`configuration_port_before.json` method records); the repository adds extra
methods (e.g., `_configuration_from_row`, `_insert_configuration_audit`) and
return annotations, which are not defects.

## 5. Structural-conformance strategy

Because the target Protocol will **not** be `@runtime_checkable`, satisfaction
is proven by deterministic signature inspection (parameter names, kinds,
order, defaults; method existence; return-annotation compatibility), not by
`isinstance`/`issubclass`.

## 6. Exact production diff

```text
app/services/configuration.py
  + from typing import Protocol          (stdlib import block)
  - class ConfigurationPort:
  + class ConfigurationPort(Protocol):
```

No other production file changes. The `SERVICE_API_DIFF_ALLOWLIST` already
contains `app/services/configuration.py` (added by H2B), so the E-parity
mechanism needs no change.

## 7. Contract-count expectations

```text
Total persistence-related contracts: 41 -> 41
Active persistence contracts:        41 -> 41
Unused legacy contracts:              0 -> 0
typing.Protocol contracts:            40 -> 41
plain structural contracts:            1 -> 0
runtime-checkable count:              36 -> 36 (unchanged)
```

## 8. Runtime-checkable decision

`@runtime_checkable` is **not** added: no production or test path performs a
supported runtime `isinstance`/`issubclass` against `ConfigurationPort`, and
the authorization requires the marker only if current source proves a runtime
check needs it. The expected result is `ConfigurationPort` is a Protocol and
is not runtime-checkable.

## 9. Exact changed files

```text
app/services/configuration.py                      (2-line production diff)
tests/test_v095h2d1_configuration_port_protocol.py (new focused test)
tests/test_v095h2a_removed_contracts.py            (minimal mapping, if required)
verification/v0.9.5-h2d1/configuration_port_before.json
verification/v0.9.5-h2d1/configuration_port_after.json
```

The existing isolated runner `verification/v0.9.5-h2a/isolated_pytest_runner.py`
is reused. Documentation: this SPEC, `RUN_VERIFICATION_V0.9.5_H2D1.md`,
`CHANGELOG.md`, `PROJECT_STATE.md`, `MASTER_ROADMAP.md`,
`CURRENT_TASK_STATE.md`, `DECISION_LOG.md`.

## 10. Focused verification plan

Layer 1 static (compile/import/circular-import/diff-check/encoding); Layer 2
representation (`ConfigurationPort` is `typing.Protocol`, not
runtime-checkable, exactly one definition, same module, no alias, no other
contract changed); Layer 3 signature parity (seven methods, parameter
names/kinds/order/defaults, `ConfigurationService` annotation still resolves
to `ConfigurationPort`, no Service executable statement changed); Layer 4
structural satisfaction of `SQLiteConfigurationRepository` by signature
inspection; Layer 5 runtime behavior (H2B configuration flow test + F2/F3
application-construction tests with the same facade-owned Repository
instance); Layer 6 accumulated F2-H2C suite (217 baseline + new tests).

## 11. Database/provider isolation

Every test/process capable of constructing `Database` runs with
`PYTHON_DOTENV_DISABLED=1`, `DATABASE_URL` absent,
`DATABASE_PATH=<fresh unique temporary database>`, `LLM_PROVIDER=local`;
resolved path printed and asserted inside the temp directory; development
database SHA-256/size/mtime verified before and after every write-capable run
and never opened; ports 8000/8501 free before and after; temp databases and
processes removed.

## 12. Rollback plan

Revert the two production lines (`class ConfigurationPort(Protocol):` ->
`class ConfigurationPort:`; drop the `typing` import if unused), remove the
new test and artifacts. Purely source-level; no database, SQL, or transaction
state is touched.

## 13. Stop conditions

Create `docs/development/BLOCKER_REPORT_V0.9.5_H2D1.md` and stop if:
`ConfigurationPort` is already a Protocol; more than one active definition
exists; it is instantiated; a runtime implementation inherits it; a supported
runtime check depends on ordinary-class behavior; conversion requires
`@runtime_checkable`, a method-signature change, `ConfigurationService` logic
change, `SQLiteConfigurationRepository` change, composition change, API
Port/dependency change, contract-count change, circular import, or a new
type-checking dependency; the development database changes; or completion
requires H2D2/H2E.

A known full-core lifecycle-race failure is not an implementation blocker but
prevents final verification closure.

## 14. Deferred H2D2/H2E work

- H2D2 (API dependency annotations / `deps.py` typing): not started.
- H2E (final contract freeze and documentation): not started.

H2D1 stops after its final report; no later stage begins without separate
owner authorization.

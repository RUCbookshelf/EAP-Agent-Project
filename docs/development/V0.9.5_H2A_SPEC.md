# v0.9.5-H2A - Remove Unused Legacy Persistence Contracts

**Status:** APPROVED / IN EXECUTION
**Baseline:** branch `master`, HEAD `760193f` (v0.9.5-H1 audit commit); H1 audit is authoritative evidence (`docs/development/PROTOCOL_CONSOLIDATION_AUDIT_V0.9.5_H1.md`, `verification/v0.9.5-h1/*`).
**Stage type:** behavior-preserving dead-contract removal. No active contract, Repository implementation, Service constructor, Router dependency, composition path, SQL, transaction, API, schema, provider, prompt, UI, or localization change.

## 1. Exact 13-contract removal list

Extracted from `verification/v0.9.5-h1/protocol_inventory.json` (classification G = stale/unused) and cross-checked against the H2A unit of `verification/v0.9.5-h1/h2_candidate_plan.json`; reconciled against current source with one bounded AST/import scan.

| # | Contract | Kind | Former definition | Former line | Declared methods / members |
| --- | --- | --- | --- | --- | --- |
| 1 | `StudentRepository` | protocol | app.repositories.protocols | 11 | get_student |
| 2 | `EssayRepository` | protocol | app.repositories.protocols | 15 | save_essay, get_submission_bundle, list_student_submissions |
| 3 | `MetricRepository` | protocol | app.repositories.protocols | 21 | save_analysis, save_analysis_run, list_analysis_runs, get_latest_analysis_run, list_analysis_units |
| 4 | `ErrorAnnotationRepository` | protocol | app.repositories.protocols | 29 | save_error_annotations, list_error_annotations |
| 5 | `DiagnosisRepository` | protocol | app.repositories.protocols | 34 | save_diagnosis |
| 6 | `FeedbackRepository` | protocol | app.repositories.protocols | 38 | save_feedback |
| 7 | `ExerciseRepository` | protocol | app.repositories.protocols | 42 | get_exercises |
| 8 | `LearnerHistoryRepository` | protocol | app.repositories.protocols | 46 | prior_records, save_history, list_student_history |
| 9 | `LearnerProfileRepository` | protocol | app.repositories.protocols | 52 | get_latest_learner_profile, save_learner_profile_snapshot, list_learner_profile_snapshots, list_longitudinal_records, list_visualization_records, list_history_evidence |
| 10 | `ConfigurationRepository` | protocol | app.repositories.protocols | 74 | ping, migration_version |
| 11 | `SystemVersionRepository` | protocol | app.repositories.protocols | 79 | record_versions, get_system_versions |
| 12 | `SubmissionRepositories` | alias | app.repositories.protocols | 84 | union of 11 Protocols |
| 13 | `SubmissionRepository` | combined_class | app.services.submission | 78 | inherits 6 stale central Protocols |

## 2. Definition locations

- `app/repositories/protocols.py` (12 definitions: 11 stale central Protocols + `SubmissionRepositories` union alias)
- `app/services/submission.py` (1 definition: legacy `SubmissionRepository` combined class)

## 3. Zero-consumer evidence

Bounded AST/import scan at HEAD `760193f` over all non-ignored Python modules (conflict files, H1 artifacts, and the H2A working directory excluded):

- `active runtime consumer count`: **0 for all 13 candidates** (no production constructor/field/parameter/return annotation, no Service/Router/dependency/script/verification import, no `isinstance`/`issubclass` check, no inheritance, no string annotation, no dynamic import).
- The only references found are: obsolete re-exports in `app/repositories/__init__.py` (import block + `__all__`, lines 1-19), the legacy class's own base references and the import block in `app/services/submission.py` (lines 15-23, 78), negative test assertions (`tests/test_v095f2_service_narrowing.py:109`, `tests/test_v095f6c_submission_service_narrowing.py:240,266,268`), and historical string keys in `verification/v0.9.5-e/*` artifact builders (untouched historical evidence).
- `app/services/configuration.py:25` annotation `ConfigurationRepository` resolves to the module-local 7-method class (no import from `app.repositories`), not the removed central Protocol.
- `replacement_contract`: **none** for every candidate (F2-G consumer-owned Ports and aggregate Repository composition already replaced these dependency boundaries).

## 4. Obsolete import / re-export references (removal targets)

- `app/repositories/__init__.py`: remove `ConfigurationRepository`, `DiagnosisRepository`, `EssayRepository`, `ExerciseRepository`, `FeedbackRepository`, `LearnerHistoryRepository`, `LearnerProfileRepository`, `MetricRepository`, `StudentRepository`, `SystemVersionRepository` from the `from .protocols import (...)` block and from `__all__`; keep only `RevisionRepository`.
- `app/services/submission.py`: remove the `from app.repositories import (DiagnosisRepository, EssayRepository, FeedbackRepository, LearnerHistoryRepository, MetricRepository, SystemVersionRepository)` block (used only as bases of the legacy class) and the `SubmissionRepository` class definition (lines 78-84 incl. docstring).
- `app/repositories/protocols.py`: remove the 11 Protocol classes and the `SubmissionRepositories` alias, plus imports used only by them: `from app.core import LearnerProfileSnapshot`, `from app.models import (AnalysisResult, DiagnosisResult, EssaySubmission, HistoryResult, ProviderResult)`, `from app.calf import ErrorAnnotation`. Keep `from typing import Any, Protocol` and `from app.revision import RevisionGroup, RevisionSnapshot` (used by `RevisionRepository`).

## 5. Exact affected files

Production (3):

- `app/repositories/protocols.py`
- `app/repositories/__init__.py`
- `app/services/submission.py`

Tests (1 new):

- `tests/test_v095h2a_removed_contracts.py` (new focused H2A removal + contract-preservation tests)

Existing tests require no edits: the only test references are negative assertions (`LearnerHistoryRepository` absent from annotations; `SubmissionRepository` absent from annotations/constructor/factory), which remain true after removal.

Verification artifacts (new):

- `verification/v0.9.5-h2a/removed_contracts.json`
- `verification/v0.9.5-h2a/remaining_contract_inventory.json`
- `verification/v0.9.5-h2a/isolated_pytest_runner.py`

## 6. Active contracts explicitly frozen (must remain byte-identical semantics)

All 42 H1-active contracts (A/B/C): the ten API-owned Ports (`app/api/ports.py`), the 24 Service-owned consumer Ports (including the local 7-method `ConfigurationRepository` in `app/services/configuration.py` - not renamed, not converted to Protocol), the three Practice Ports, `RevisionRepository` (central), and the four infrastructure-internal readers (`_SubmissionBundleReader`, `_AnalysisRunReader` x2, `_DiagnosticCalibrationReader`). No `@runtime_checkable` change; no method-set/signature/order/parameter/default/return-annotation change; no module relocation; no consolidation of repeated declarations (`get_submission_bundle`, `get_latest_analysis_run`, `get_student`, `get_configuration`, `get_active_configuration`, `list_visualization_records`).

## 7. Test update plan

- New `tests/test_v095h2a_removed_contracts.py` proves: (1) all 13 removed names absent from former modules; (2) obsolete re-exports absent (`app.repositories.__all__` == active set); (3) no active source import references a removed name; (4) all 42 H1-active contracts remain defined with exact method sets; (5) intended concrete Repositories still satisfy active contracts (runtime structural checks); (6) `SubmissionService` still uses exactly its four F6C Ports; (7) local `ConfigurationRepository` contract unchanged; (8) Practice read/write Port separation unchanged; (9) the ten API-owned Ports unchanged.
- No existing behavior test is deleted or weakened; negative assertions in F2/F6C tests stay untouched.

## 8. Database / provider isolation

For every process capable of constructing `Database`: `PYTHON_DOTENV_DISABLED=1`, `DATABASE_URL` removed, `DATABASE_PATH` = fresh DB inside a unique temporary directory, `LLM_PROVIDER=local`; resolve and assert the effective path is inside the temporary directory and is not the development database; execute only local/fake-provider tests; close connections/processes; remove the temporary directory; confirm ports 8000/8501 free. Development database SHA-256/size/mtime compared before and after every write-capable run; never opened. No live external-provider calls.

## 9. Rollback plan

The implementation commit is fully reversible in one `git revert` of `refactor(v0.9.5-h2a)` (pure definition/import deletion; no data or behavior migration). If any active contract is incorrectly selected, restore it immediately from the pre-change snapshot and update the manifest honestly.

## 10. Stop conditions (create `docs/development/BLOCKER_REPORT_V0.9.5_H2A.md`)

- candidate count != exactly 13; any candidate with an active runtime consumer; replacement contract required; Service constructor / Router dependency / application composition change required; Repository implementation or signature change required; SQL/transaction/schema/migration change required; renaming or relocating an active contract required; merging active duplicate contracts required; runtime-checkable behavior change required; external supported compatibility obligation found; development database changes; completion requires beginning H2B-H2E.

## 11. Deferred H2B-H2E work (not begun)

- H2B: rename the active local `ConfigurationRepository` (collision resolution).
- H2C: canonicalize the `_AnalysisRunReader` duplicate pair.
- H2D: formalize API Port production annotations / plain-class-to-Protocol conversion.
- H2E: final contract freeze and documentation.

The Configuration same-name collision is resolved in H2A only through removal of the stale central `ConfigurationRepository`.

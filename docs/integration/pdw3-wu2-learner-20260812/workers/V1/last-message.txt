# RETRY-2 V1 — Independent Read-Only Verifier: **CONCERNS**

No FAIL-level defect found in the implementation or scope. The two blockers reported by Worker D are confirmed real but are unowned cross-cutting edits (INT/parent-owned), and independent test execution was not possible in this read-only session — see the limitation below.

## What I verified independently (read-only)

**Git / scope — PASS.** Branch `dept/feedback-learner`, HEAD `7a9e4b470c41c0453a3795233f1bdd5c483d80ae` (exact packet baseline). Tracked diff is exactly 4 files, purely additive (+137/−0): [main.py](/A:/EAP%20Agent%20Project/worktrees/learner/app/api/main.py:81) (ack router registered once, two service-graph keys, state assignment), [deps.py](/A:/EAP%20Agent%20Project/worktrees/learner/app/api/deps.py:118), [service.py](/A:/EAP%20Agent%20Project/worktrees/learner/app/journey/service.py:451) (two additive projection methods only), [test_composition_root.py](/A:/EAP%20Agent%20Project/worktrees/learner/tests/test_composition_root.py:31). No `app/database` changes, no migrations, no `app/review` package in LEARNER, no `app/infrastructure/learner_review.py`. The 5 pre-existing untracked paths are present; the 4 with WU0 baselines match SHA-256 byte-for-byte (the 5th, the PDW3-ALIGN report, has no recorded baseline — noted). `shared-core` remains at its exact 35-entry CORE WU1 state with no RETRY-2 traces; no other worktree touched.

**Implementation claims — PASS (static).** [review_bridge.py](/A:/EAP%20Agent%20Project/worktrees/learner/app/learner/review_bridge.py) is a structural CORE mirror with reserved-provenance enforcement and UTC fail-closed checks; [review_transfer.py](/A:/EAP%20Agent%20Project/worktrees/learner/app/practice/review_transfer.py) keeps `evidence_kind="practice"` literal, forwards the three rating channels separately (no averaging), carries rating-rule version + scheduler identity into provenance, and propagates CORE rejections unchanged. [transfer.py](/A:/EAP%20Agent%20Project/worktrees/learner/app/journey/transfer.py) keeps the two projections in separate sections with verbatim statuses/comparability and no causal language. [acknowledgement.py](/A:/EAP%20Agent%20Project/worktrees/learner/app/learner/acknowledgement.py) implements the 10 fail-closed gates (consent → ids → kind → status → provenance → versions → source records/ownership/L0 → normative/causal → duplicate/conflict), all before the single append. `outcome_claim` is locked to `"none"`, epistemic status to L0, and the token scan finds only denial/limitation strings — no mastery/proficiency/ability/learning-gain claims.

**Composition probe (mine, read-only):** 94 method/path pairs excluding auto HEAD/OPTIONS (102 with them, matching D's recorded count), `duplicate_pairs: {}`, ack pairs `POST+GET /api/v1/students/{student_id}/acknowledgements` registered exactly once; ack router in `_BUSINESS_ROUTERS` exactly once. No `Database(`/`sqlite3`/migration usage in any new file — one database authority holds.

**Blocker 1 confirmed:** B's projections are service-level only — no `practice-history`/`authentic-application` route exists anywhere in `app/api` (requires the unowned [journey.py](/A:/EAP%20Agent%20Project/worktrees/learner/app/api/routers/journey.py) edit).

**Blocker 2 confirmed:** [test_v095b_router_contract.py](/A:/EAP%20Agent%20Project/worktrees/learner/tests/test_v095b_router_contract.py:24) pins a router tuple without the ack router (unchanged file; the new failure is caused by the packet-required inclusion). [test_v095d_api_contract.py](/A:/EAP%20Agent%20Project/worktrees/learner/tests/test_v095d_api_contract.py:157) pins exactly 81 endpoints and is unchanged; WU2 adds exactly 2 pairs, and D's recorded pre-change baseline already showed 100 vs 81 — the mismatch is pre-existing, not WU2-caused. Both need parent/INT-owned contract regeneration.

## Test-execution limitation (why CONCERNS, not PASS)

This session is fully read-only: pytest cannot even start (`FileNotFoundError: No usable temporary directory` — no writable temp location, including system temp), and the `codex exec` fallback is impossible from here (CLI fell back to `api.openai.com`; sockets denied, `os error 10013`). So I could not independently execute the suites. I relied on recorded evidence, which is consistent across multiple independent runs: A 40/40; B 18 focused + 439 + 249; C 51/51 + 250 + 265; D 121 focused + 28 composition pins (the 2 expected failures) + 396 + 251; parent quiet-worktree gate 109 passed ([checkpoint 007](/A:/EAP%20Agent%20Project/worktrees/learner/docs/integration/pdw3-wu2-learner-20260812/CHECKPOINT-007-RETRY2-ABC-PARENT-VERIFIED.md)), D audit [checkpoint 009](/A:/EAP%20Agent%20Project/worktrees/learner/docs/integration/pdw3-wu2-learner-20260812/CHECKPOINT-009-RETRY2-D-DONE-PARENT-AUDIT.md). First-attempt sandbox BLOCKED messages for A/B/C are preserved as documented.

## For the parent's handoff

Carry both D blockers (journey route exposure; v095b/v095d contract pins) plus the INT-gated boundaries (acknowledgement persistence + production evidence lookup → 503 by design; CORE review service injection → `core_review_service_missing` by design). Non-blocking observations: `activity_id="PA-PENDING"` placeholder default in the bridge record, and an `int()` cast on `essay_id` in `transfer.py`. I could not persist a findings file in this read-only sandbox — please persist this verdict under `workers/V1/` alongside the existing empty dispatch outputs.

